// HelloWorld/EmptyProgram.kt

/**
 *
 * keywords : 프로그램 언어에서 중요한 의미를 가짐
 *          "fun" == function
 *
 * function : function의 이름으로 실행되는 코드의 모음
 *
 *          fun functionName() { <- start of function body
 *
 *              collection of code in it! <- function body
 *
 *          } <- end of function body
 *
 * - function body contains [Statements] and [Expressions]
 *      - Statements : produces an effect. (변화에 대한 결과값을 생산하는 것)
 *      - Expressions : yields a result.(결과를 산출하는 것)
 *
 */
fun main() {
  // Program code here ... <-- 프로그램에서 현재 라인은 무시함 (주석)
}